/- Proteus hard-coded functions
struct GLOBAL{
 /-   me int[map string]: hardFuncNames<- {"innr":1, "outr":1, "size":1, "first":1, "type":1, "sin":1, "cos":1, "tan":1, "time":1}

    me bool: isHardFunction(me string: tag) <- {
        if(tag=="innr"){  /- hardFuncNames.containsKey(tag)
            return(true)
        } else {return(false)}
    }
}
