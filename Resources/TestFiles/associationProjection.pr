{
    #value = _
    #measure = {value}
    #thing = {}
    #Holder = thing:{}^measure={value:7}

    // A bare definition use materializes one occurrence.  A singleton list
    // would introduce an additional S-container rather than trigger evaluation.
    Holder
}
